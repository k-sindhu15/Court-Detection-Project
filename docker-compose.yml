version: "3.8"

services:
  badminton-court-detection:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
      - ./config:/app/config
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
